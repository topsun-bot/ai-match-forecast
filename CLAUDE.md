# Project context

Python 3.11+ report generator; static HTML is published to GitHub Pages.
Run `python3 -m unittest discover -s tests -v` without keys or live network calls.
Use explicit mock mode in development. Never publish mock output as a live report.
Shared project guidance is maintained in AGENTS.md; review-only requirements are
written directly in REVIEW.md. CI and GitHub approval rules determine merge eligibility.
