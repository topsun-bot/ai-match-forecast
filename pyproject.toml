[tool.ruff]
target-version = "py311"
extend-exclude = ["_site", ".site-history"]

[tool.ruff.lint]
# Start with executable correctness, not a repository-wide formatting rewrite.
select = ["E9", "F63", "F7", "F82"]
